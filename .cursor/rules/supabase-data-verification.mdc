---
description: Supabase 데이터 검증을 위한 psql 연결 정보
globs: ["docs/sql/**/*.sql", "docs/**/*.md"]
alwaysApply: true
---

Supabase 데이터 검증 시 다음 psql 연결 문자열을 사용하여 데이터를 확인할 것:

```bash
psql "postgresql://postgres.xyqpggpilgcdsawuvpzn:ZNDqDunnaydr0aFQ@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres"
```

## 사용 예시

- SQL 파일 실행 (프로젝트 루트에서):
  ```bash
  psql "postgresql://..." -f docs/sql/[파일명].sql
  ```

- 직접 쿼리 실행:
  ```bash
  psql "postgresql://..." -c "SELECT * FROM schema_name.table_name;"
  ```